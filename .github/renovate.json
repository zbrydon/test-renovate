{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "dependencyDashboard": true,
  "configMigration": true,
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Route public.ecr.aws / registry.k8s.io / quay.io / docker.io images through the ECR pull-through cache",
      "matchDatasources": ["docker"],
      "matchFileNames": ["**/Dockerfile*"],
      "matchPackageNames": [
        "public.ecr.aws/**",
        "registry.k8s.io/**",
        "quay.io/**",
        "docker.io/**"
      ],
      "replacementNameTemplate": "111111111111.dkr.ecr.ap-southeast-2.amazonaws.com/{{{packageName}}}"
    },
    {
      "description": "Route bare Docker Hub images through the ECR pull-through cache (adds docker.io, plus library/ for official images)",
      "matchDatasources": ["docker"],
      "matchFileNames": ["**/Dockerfile*"],
      "matchPackageNames": ["/^[^./]+(\\/.+)?$/"],
      "replacementNameTemplate": "111111111111.dkr.ecr.ap-southeast-2.amazonaws.com/docker.io/{{ replace '^([^/]+)$' 'library/$1' packageName }}"
    },
    {
      "description": "Consolidate all ECR pull-through cache replacements into a single PR",
      "matchFileNames": ["**/Dockerfile*"],
      "matchUpdateTypes": ["replacement"],
      "groupName": "ECR pull-through cache migration",
      "branchTopic": "docker-ecr-pull-through-cache"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/(^|/)serverless(\\..*)?\\.ya?ml$/"],
      "matchStrings": [
        "frameworkVersion:\\s*['\"](?<currentValue>[^'\"]+)['\"]"
      ],
      "depNameTemplate": "serverless",
      "datasourceTemplate": "npm"
    }
  ],
  "postUpdateOptions": ["yarnDedupeFewer", "pnpmDedupe"],
  "prCreation": "immediate",
  "prHourlyLimit": 10,
  "semanticCommitScope": "",
  "semanticCommitType": "deps"
}
